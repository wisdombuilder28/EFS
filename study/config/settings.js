// App-wide config knobs. Tweak here, not in deep modules.
export const SETTINGS = {
  maxInputLength: 2000,
  minInputLength: 1,
  typingSpeedMs: 8,        // per character for the typing effect
  thinkingLabel: "Thinking",
  errorMessage: "Something went wrong. Please try again.",
  appName: "EFS AI",
};
